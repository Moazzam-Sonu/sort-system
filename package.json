{
  "name": "shopify-collection-range-sorter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Web interface for sorting Shopify collections by range or Shopify sort order.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "db:migrate": "node src/database/migrate.js",
    "users:create": "node src/database/create-user.js",
    "sort:range": "node src/reorder-collection.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "sweetalert2": "^11.26.25"
  }
}
